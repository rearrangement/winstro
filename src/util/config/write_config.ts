import colors from '../colors'

function write_config() {
    // todo: include config generator which automatically detects
    // all installed packages and writes a config based off of that
}

export default write_config